"""Vector search helpers.

Face identity search lives in vector.repository.FaceRepository (register/lookup).
This module is reserved for future non-identity vector search (e.g. memory embedding
retrieval) — kept empty deliberately; YAGNI until that exists.
"""
